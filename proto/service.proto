syntax = "proto3";

package proto;

option go_package = "dummy/proto";

message Input {
    int64 a = 1;
    int64 b = 2;
}

message Output {
    int64 res = 1;
}

service Multiplication {
    rpc Mul (Input) returns (Output);
}
